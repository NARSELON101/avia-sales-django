services:
  rabbitmq:
    image: rabbitmq:3.10.7-management
    ports:
      - "15672:15672"
      - "5672:5672"
  email_worker:
    image: avia-sales-email:latest
    environment:
      - CELERY_BROKER_URL=amqp://guest:guest@rabbitmq:5672//
    depends_on:
      - rabbitmq

  notification_watcher:
    image: avia-sales-notification-watcher:latest
    volumes:
      - db-data:/app/celery_notification_watcher/db
    environment:
      - CELERY_BROKER_URL=amqp://guest:guest@rabbitmq:5672//
    depends_on:
      - rabbitmq
      - app

  notification_beat:
    image: avia-sales-beat:latest
    environment:
      - CELERY_BROKER_URL=amqp://guest:guest@rabbitmq:5672//
    depends_on:
      - rabbitmq


  app:
    image: avia-sales-app:latest
    ports:
      - "8000:8000"
    volumes:
      - db-data:/app/db
    environment:
      - CELERY_BROKER_URL=amqp://guest:guest@rabbitmq:5672//

volumes:
  db-data: